{{VERBOSITY_INSTRUCTIONS}}
## Planning — Adversarial Self-Critique and Publish

In the previous turn of this same session you produced a **draft plan** for issue #{{ISSUE_NUMBER}} in {{REPO}}: a set of proposed sub-issues with titles, bodies, descriptive labels, and dependencies. Work from that draft — do not start from scratch.

Your job now is to make the plan stronger by attacking it, revising it once, and only then publishing the final sub-issues.

### Step 1 — Attack your own draft

Adversarially critique your draft. Ask **"what's wrong with this approach?"** and be specific and harsh. Consider at least:

- **Missing work** — asks in the issue with no sub-issue covering them; tests, docs, migrations, or rollout steps omitted.
- **Mis-scoping** — sub-issues too large for a single ~500-line PR, or so small they should be merged; tasks spanning unrelated modules.
- **Dependencies** — wrong, missing, or circular `Depends on #N` links; work ordered so it cannot compile or test.
- **Over-engineering** — speculative sub-issues, premature abstraction, or scope the issue never asked for (KISS).
- **Duplication** — sub-issues that recreate an existing open issue you listed earlier.
- **Weak acceptance criteria** — criteria that are not specific or testable.

### Step 2 — Revise once

Revise the plan a single time to address the strongest criticisms. Merge, split, re-scope, re-order, drop, or add sub-issues as needed. This is a single revision pass — do not loop.

### Step 3 — Publish the final plan only

Now, and only now, create the **final, revised** sub-issues as real GitHub issues with the `gh` CLI. The worker validates your output for evidence that sub-issues were created; describing a plan in text alone does not count as complete.

- On sub-issues add only descriptive labels (`bug`, `enhancement`, `documentation`). Do not add reserved workflow labels (`top-priority`, `work-on`, `low-priority`, `needs-human`, `failed`, `failed-once`, `refine-issue`, `planning`, `question`, `best-model`) — the planner is not on the trusted-author allowlist, so any reserved label is silently stripped by the `label_security` check.
- Every sub-issue body must include `Part of #{{ISSUE_NUMBER}}`, testable acceptance criteria, and any `Depends on #N` links.

{{MILESTONE_INSTRUCTIONS}}

Create sub-issues in implementation order, for example:

```bash
gh issue create --repo {{REPO}} --title "Sub-task title" --body "Description

Depends on

Part of #{{ISSUE_NUMBER}}" --label "enhancement"
```

Then post **one** summary comment on issue #{{ISSUE_NUMBER}} listing the sub-issues created (with links), the suggested implementation order, the dependency relationships (include a Mermaid `flowchart` of the dependency graph when there are three or more sub-issues), and any assumptions made.

### Do not publish the critique

Your critique from Step 1 is internal reasoning only. **Do not post the critique anywhere** — not as a comment, not in a sub-issue body, not in the summary. Publish only the final, revised sub-issues and the summary comment. Do not leave behind a draft set of sub-issues from before the revision.

### Final step: close this planning issue

Your very last action must be to close this issue, immediately after the summary comment — do not run any other step in between. Close it as `completed` whether you created zero, one, or many sub-issues:

```bash
gh issue close {{ISSUE_NUMBER}} --repo {{REPO}} --reason completed
```

The `{{PLANNING_LABEL}}` label removal stays with the worker. The worker keeps a safety-net close, but your inline close is the source of truth and must always run. The user can reopen this issue to iterate on the plan.
